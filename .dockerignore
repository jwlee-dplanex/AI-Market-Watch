# 로컬 전용 — 이미지에 들어가면 안 됨
venv/
.venv/
node_modules/
.git/
.env
*.pem

# 이미지 빌드에 불필요 (COPY . . 로 통째로 넣기 전 제외)
__pycache__/
*.pyc
.pytest_cache/
.ra_tmp/

# collectstatic이 컨테이너 안에서 새로 만드는 산출물 — 빌드 컨텍스트에 넣을 이유 없음
staticfiles/

# 문서·연구 산출물 — 앱 실행에 불필요, 이미지 용량만 늘림
docs/
research/
README.md
